#pragma once

// Comment out this define to disable sample output and timing statistics.
#define UART_HOST_PRINT_SAMPLES

// Print every Nth received sample: 200 Hz / 10 = nominally 20 Hz.
#define UART_HOST_PRINT_EVERY_N 10
