`host_config.h` controls sample output and the number of samples per printout.
Rebuild after changing these compile-time settings.

Application-wide headers belong here. The SerialPort interface lives alongside
its implementation in `lib/SerialPort/`.
